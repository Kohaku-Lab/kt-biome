name: swe
version: "1.0"
base_config: "@kt-biome/creatures/general"

controller:
  # Default: uses kt model default. Override with --llm gpt-5.4
  reasoning_effort: "${REASONING:high}"

system_prompt_file: prompts/system.md

# ``skills`` inherited from ``general`` is ``["*"]`` — every installed
# package skill auto-enables. SWE particularly benefits from
# `git-commit-flow` (path-activates on source files) and `todo-file`.
